# Headscale environment configuration
# Copy to app.env and customize before starting
# Main configuration is done via volumes/config/headscale/config.yaml

# Timezone
TZ=America/New_York

# Public hostname (must be reachable by Tailscale clients)
BASE_HOST_NAME=headscale.example.com
